// 'use strict';

// import log from '@zero-cli-dev/log';

function init(projectName, cmdObj) {
    console.log('init',cmdObj);
    // log.info('init', projectName, cmdObj.force);
}

export default init;